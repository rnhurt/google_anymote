#!/usr/bin/env ruby

require 'google_anymote'
cert  = File.read 'cert.pem'
pair = GoogleAnymote::Pair.new(cert, 'NSXGT1-8064773.local.', 'test', 'test')
pair.start_pairing

# Collect pairing code
print 'Enter the code from the TV: '
code = gets.chomp

pair.complete_pairing(code)
